#include<stdio.h>
#include<string.h>
#include<ctype.h>

int main(){

        char arr[30],*ptr,*pd;
        gets(arr);
        int len=strlen(arr),cnt=1;
	ptr=arr;

        int num=0,dec=0;
	pd=strchr(arr,'.');
	if(pd==NULL)
	{
	printf("ERROR");
	return 0;
	}

	if(*ptr=='-'){
	num=ato
	
	}
	else{
	
	}


}
